version: v1.0
name: Linux Tests

agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804

blocks:
  - name: '/kernel tests'
    dependencies: []
    run:
      when: "change_in('/kernel', {pipeline_file: 'ignore'})"
    task:
      jobs:
        - name: 'Test'
          commands:
            - checkout

  - name: '/arch tests'
    dependencies: []
    run:
      when: "change_in('/arch', {pipeline_file: 'ignore'})"
    task:
      jobs:
        - name: 'Test'
          commands:
            - checkout
